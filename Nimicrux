-------------------------------------------HTML------------------------------------------------------------
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>NimiCrux – Developer Platform</title>

  <!-- Font -->
  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;600;700&display=swap" rel="stylesheet">

  <link rel="stylesheet" href="style.css" />
</head>
<body>

  <!-- NAVBAR -->
  <header class="navbar">
    <h1 class="logo">Nimi<span>Crux</span></h1>
    <nav>
      <a href="#">Home</a>
      <a href="#">Features</a>
      <a href="#">Community</a>
      <a href="#">Login</a>
      <button class="toggle-btn" onclick="toggleTheme()">🌙</button>
    </nav>
  </header>

  <!-- HERO -->
  <section class="hero reveal">
    <h2 class="typing"></h2>
    <p class="tagline">Build. Learn. Evolve.</p>
    <button onclick="showMessage()">Get Started</button>
  </section>

  <!-- LOGO CAROUSEL -->
  <section class="logos reveal">
  <h2>Popular Tech & Data Science Tools</h2>
  <div class="logo-carousel">
    <img src="https://upload.wikimedia.org/wikipedia/commons/c/c3/Python-logo-notext.svg" alt="Python" />
    <img src="https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png" alt="GitHub" />
    <img src="https://upload.wikimedia.org/wikipedia/commons/9/93/Amazon_Web_Services_Logo.svg" alt="AWS" />
    <img src="https://upload.wikimedia.org/wikipedia/commons/2/2d/Tensorflow_logo.svg" alt="TensorFlow" />
    <img src="https://upload.wikimedia.org/wikipedia/commons/4/44/Microsoft_logo.svg" alt="Microsoft" />
    <img src="https://numpy.org/images/logo.svg" alt="NumPy" />
  </div>
</section>


  <!-- STATS -->
  <section class="stats reveal">
    <div class="stat">
      <h3 data-count="5000">0</h3>
      <p>Developers</p>
    </div>
    <div class="stat">
      <h3 data-count="120">0</h3>
      <p>Resources</p>
    </div>
    <div class="stat">
      <h3 data-count="50">0</h3>
      <p>Tools</p>
    </div>
  </section>

  <!-- FEATURES -->
  <section class="features reveal">
    <h2>Why NimiCrux?</h2>
    <div class="feature-grid">
      <div class="feature-card reveal">⚡ Fast Learning Resources</div>
      <div class="feature-card reveal">🌐 Developer Community</div>
      <div class="feature-card reveal">🛠 Modern Tools</div>
      <div class="feature-card reveal">📈 Career Growth</div>
    </div>
  </section>
<!-- ABOUT SECTION -->
<section class="about reveal">
  <h2>About NimiCrux</h2>
  <p>
    NimiCrux is a dynamic developer platform where you can learn fast, connect with a passionate community, and access the latest tools to boost your coding and data science skills. With an electric-themed design and interactive features, NimiCrux inspires you to build, grow, and thrive in tech. Join us and start shaping the future today!
  </p>
</section>


 <!-- AUTH SECTION -->
<section class="auth-section reveal">
  <div class="auth-container">

    <!-- LOGIN FORM -->
    <div class="auth-card" id="login-card">
      <h2>Login</h2>
      <form onsubmit="login(event)">
        <input type="email" placeholder="Email" required />
        <input type="password" placeholder="Password" required />
        <button type="submit">Login</button>
        <p class="switch-text">Don't have an account? <span onclick="toggleForm()">Register</span></p>
      </form>
    </div>

    <!-- REGISTER FORM -->
    <div class="auth-card" id="register-card">
      <h2>Register</h2>
      <form onsubmit="register(event)">
        <input type="text" placeholder="Full Name" required />
        <input type="email" placeholder="Email" required />
        <input type="password" placeholder="Password" required />
        <input type="password" placeholder="Confirm Password" required />
        <button type="submit">Register</button>
        <p class="switch-text">Already have an account? <span onclick="toggleForm()">Login</span></p>
      </form>
    </div>

  </div>
</section>
<!-- Embedded AI Dictionary Section -->
<section class="dictionary reveal">
  <h2>Minimi AI Dictionary</h2>
  <p>Look up any word instantly below:</p>

  <div class="dictionary-box">
    <input type="text" id="wordInput" placeholder="Enter a word..." />
    <button onclick="searchWord()">Search</button>
  </div>

  <div id="definition" class="definition"></div>
</section>

  <!-- CTA -->
  <section class="cta reveal">
    <h2>Ready to start your journey?</h2>
    <button>Join the Community</button>
  </section>

  <!-- FOOTER -->
  <footer>
    <p>© 2025 NimiCrux. Built for developers.</p>
  </footer>

  <script src="script.js"></script>
</body>
</html>
---------------------------------CSS------------------------------------------------------
:root {
  --bg: #000000;
  --text: #ffffff;
  --card: #020617;
  --green: #22c55e;
}

.light {
  --bg: #ffffff;
  --text: #000000;
  --card: #f1f5f9;
  --green: #16a34a;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'JetBrains Mono', monospace;
}

body {
  background: var(--bg);
  color: var(--text);
  transition: 0.4s;
}

/* NAVBAR */
.navbar {
  display: flex;
  justify-content: space-between;
  padding: 20px 40px;
  border-bottom: 1px solid var(--green);
}

.logo span {
  color: var(--green);
  text-shadow: 0 0 5px var(--green), 0 0 10px var(--green), 0 0 20px var(--green);
}

.navbar a {
  margin-left: 20px;
  color: var(--text);
  text-decoration: none;
}

.navbar a:hover {
  color: var(--green);
}

.toggle-btn {
  margin-left: 20px;
  background: none;
  border: 1px solid var(--green);
  color: var(--green);
  padding: 6px 10px;
  border-radius: 6px;
  cursor: pointer;
}

/* HERO */
.hero {
  text-align: center;
  padding: 100px 20px;
  background: linear-gradient(180deg, var(--bg), #052e16);
  border: 2px solid var(--green);
  animation: electricGlow 2s infinite alternate;
}

.hero h2 {
  font-size: 2.6rem;
  color: var(--green);
  text-shadow: 0 0 5px var(--green), 0 0 10px var(--green), 0 0 20px var(--green);
}

.tagline {
  margin: 15px 0 30px;
  color: #a7f3d0;
}

.hero button {
  padding: 14px 30px;
  background: var(--green);
  border: none;
  border-radius: 6px;
  font-weight: bold;
  cursor: pointer;
}

/* LOGO CAROUSEL */
.logos {
  text-align: center;
  padding: 60px 20px;
}

.logo-carousel {
  display: flex;
  justify-content: center;
  gap: 50px;
  overflow: hidden;
  animation: scrollLogos 20s linear infinite;
}

.logo-carousel img {
  width: 80px;
  height: 80px;
  filter: brightness(1) contrast(1.2);
  transition: transform 0.3s;
}

.logo-carousel img:hover {
  transform: scale(1.2);
  filter: drop-shadow(0 0 10px var(--green));
}

@keyframes scrollLogos {
  0% { transform: translateX(0); }
  100% { transform: translateX(-50%); }
}

/* STATS */
.stats {
  display: flex;
  justify-content: center;
  gap: 50px;
  padding: 60px 20px;
}

.stat h3 {
  font-size: 2.2rem;
  color: var(--green);
}

/* FEATURES */
.features {
  padding: 80px 20px;
  text-align: center;
}

.feature-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 20px;
  margin-top: 40px;
}

.feature-card {
  padding: 30px;
  border: 2px solid var(--green);
  border-radius: 10px;
  background: var(--card);
  transition: 0.3s;
  animation: electricGlow 2s infinite alternate;
}

.feature-card:hover {
  transform: translateY(-10px);
  animation-duration: 1s;
}

/* AUTH */
.auth {
  display: flex;
  justify-content: center;
  padding: 80px 20px;
}

.auth span {
  color: var(--green);
}

.auth form {
  background: var(--card);
  border: 2px solid var(--green);
  padding: 30px;
  border-radius: 10px;
  width: 300px;
  animation: electricGlow 2s infinite alternate;
}

.auth input {
  width: 100%;
  margin-bottom: 15px;
  padding: 12px;
  background: var(--bg);
  border: 1px solid var(--green);
  color: var(--text);
}

.auth button {
  width: 100%;
  padding: 12px;
  background: var(--green);
  border: none;
  font-weight: bold;
  cursor: pointer;
}

/* CTA */
.cta {
  text-align: center;
  padding: 80px 20px;
  background: var(--card);
  border: 2px solid var(--green);
  animation: electricGlow 2s infinite alternate;
}

.cta button {
  padding: 15px 35px;
  background: var(--green);
  border: none;
  font-weight: bold;
  cursor: pointer;
}

/* FOOTER */
footer {
  text-align: center;
  padding: 20px;
  border-top: 1px solid var(--green);
}

/* SCROLL REVEAL */
.reveal {
  opacity: 0;
  transform: translateY(40px);
  transition: all 0.8s ease-out;
}

.reveal.active {
  opacity: 1;
  transform: translateY(0);
}

/* ELECTRIC BORDER ANIMATION */
@keyframes electricGlow {
  0% {
    box-shadow: 0 0 5px var(--green), 0 0 10px var(--green), 0 0 20px var(--green), 0 0 40px var(--green);
  }
  50% {
    box-shadow: 0 0 10px #00fffc, 0 0 20px #00fffc, 0 0 30px #00fffc, 0 0 60px #00fffc;
  }
  100% {
    box-shadow: 0 0 5px var(--green), 0 0 10px var(--green), 0 0 20px var(--green), 0 0 40px var(--green);
  }
}
/* ABOUT SECTION */
.about {
  text-align: center;
  padding: 80px 20px;
  margin: 40px auto;
  background: var(--card);
  border: 2px solid var(--green);
  border-radius: 15px;
  max-width: 900px;
  animation: electricGlow 2s infinite alternate;
}

.about h2 {
  font-size: 2.5rem;
  color: var(--green);
  margin-bottom: 20px;
  text-shadow: 0 0 5px var(--green), 0 0 10px var(--green), 0 0 20px var(--green);
}

.about p {
  font-size: 1.2rem;
  line-height: 1.8;
  color: var(--text);
  margin: 0 auto;
  max-width: 700px;
}
/* AUTH SECTION */
.auth-section {
  display: flex;
  justify-content: center;
  padding: 80px 20px;
}

.auth-container {
  display: flex;
  gap: 50px;
  flex-wrap: wrap;
  justify-content: center;
}

.auth-card {
  background: var(--card);
  border: 2px solid var(--green);
  border-radius: 15px;
  padding: 30px;
  width: 300px;
  text-align: center;
  animation: electricGlow 2s infinite alternate;
  transition: transform 0.3s;
}

.auth-card h2 {
  color: var(--green);
  margin-bottom: 20px;
  text-shadow: 0 0 5px var(--green), 0 0 10px var(--green);
}

.auth-card input {
  width: 100%;
  margin-bottom: 15px;
  padding: 12px;
  background: var(--bg);
  border: 1px solid var(--green);
  color: var(--text);
  border-radius: 6px;
}

.auth-card button {
  width: 100%;
  padding: 12px;
  background: var(--green);
  border: none;
  font-weight: bold;
  cursor: pointer;
  border-radius: 6px;
}

.switch-text {
  margin-top: 15px;
  color: var(--text);
}

.switch-text span {
  color: var(--green);
  cursor: pointer;
  text-decoration: underline;
}

.auth-card:hover {
  transform: translateY(-5px);
}

/* Initially hide register card */
#register-card {
  display: none;
}

.dictionary {
  text-align: center;
  padding: 80px 20px;
  background: var(--card);
  border: 2px solid var(--green);
  border-radius: 15px;
  margin: 40px auto;
  max-width: 800px;
  animation: electricGlow 2s infinite alternate;
}

.dictionary h2 {
  color: var(--green);
  font-size: 2.5rem;
  margin-bottom: 15px;
  text-shadow: 0 0 5px var(--green), 0 0 10px var(--green);
}

.dictionary p {
  color: var(--text);
  margin-bottom: 25px;
  font-size: 1.2rem;
}

.dictionary-box {
  display: flex;
  justify-content: center;
  gap: 10px;
  flex-wrap: wrap;
  margin-bottom: 20px;
}

.dictionary-box input {
  padding: 12px 15px;
  width: 250px;
  border: 1px solid var(--green);
  border-radius: 6px;
  background: var(--bg);
  color: var(--text);
}

.dictionary-box button {
  padding: 12px 20px;
  background: var(--green);
  border: none;
  font-weight: bold;
  cursor: pointer;
  border-radius: 6px;
  transition: transform 0.3s;
}

.dictionary-box button:hover {
  transform: translateY(-3px);
}

.definition {
  margin-top: 20px;
  color: var(--text);
  font-size: 1.1rem;
  text-align: left;
  max-width: 700px;
  margin-left: auto;
  margin-right: auto;
  border-top: 1px solid var(--green);
  padding-top: 15px;
}
------------------------------------------JS----------------------------------------
/* THEME TOGGLE */
function toggleTheme() {
  document.body.classList.toggle("light");
  const btn = document.querySelector(".toggle-btn");
  const isLight = document.body.classList.contains("light");
  btn.textContent = isLight ? "🌞" : "🌙";
  localStorage.setItem("theme", isLight ? "light" : "dark");
}

window.onload = () => {
  const theme = localStorage.getItem("theme");
  const btn = document.querySelector(".toggle-btn");
  if (theme === "light") {
    document.body.classList.add("light");
    btn.textContent = "🌞";
  }
};

/* HERO MESSAGE */
function showMessage() {
  alert("Welcome to NimiCrux 🚀");
}

/* LOGIN */
function login(e) {
  e.preventDefault();
  alert("Login successful (demo only)");
}

/* TYPING EFFECT */
const text = "Build the Future with NimiCrux";
let index = 0;
const element = document.querySelector(".typing");

function typeEffect() {
  if (index < text.length) {
    element.textContent += text.charAt(index);
    index++;
    setTimeout(typeEffect, 80);
  }
}
typeEffect();

/* COUNTER */
const counters = document.querySelectorAll("[data-count]");
counters.forEach(counter => {
  let start = 0;
  const end = +counter.dataset.count;
  const interval = setInterval(() => {
    start++;
    counter.textContent = start;
    if (start === end) clearInterval(interval);
  }, 10);
});

/* SCROLL REVEAL */
function scrollReveal() {
  const reveals = document.querySelectorAll(".reveal");
  for (let i = 0; i < reveals.length; i++) {
    const windowHeight = window.innerHeight;
    const elementTop = reveals[i].getBoundingClientRect().top;
    const revealPoint = 150;

    if (elementTop < windowHeight - revealPoint) {
      reveals[i].classList.add("active");
    } else {
      reveals[i].classList.remove("active");
    }
  }
}

window.addEventListener("scroll", scrollReveal);
window.addEventListener("load", scrollReveal);

/* ELECTRIC BORDER FLICKER ON HOVER */
document.querySelectorAll(".feature-card, .cta, .auth form, .hero").forEach(el => {
  el.addEventListener("mouseenter", () => {
    el.style.animationDuration = "0.5s";
  });
  el.addEventListener("mouseleave", () => {
    el.style.animationDuration = "2s";
  });
});
// Scroll Reveal (already in your JS)
function scrollReveal() {
  const reveals = document.querySelectorAll(".reveal");
  for (let i = 0; i < reveals.length; i++) {
    const windowHeight = window.innerHeight;
    const elementTop = reveals[i].getBoundingClientRect().top;
    const revealPoint = 150;

    if (elementTop < windowHeight - revealPoint) {
      reveals[i].classList.add("active");
    } else {
      reveals[i].classList.remove("active");
    }
  }
}

window.addEventListener("scroll", scrollReveal);
window.addEventListener("load", scrollReveal);
// Toggle between Login and Register forms
function toggleForm() {
  const loginCard = document.getElementById("login-card");
  const registerCard = document.getElementById("register-card");

  if (loginCard.style.display === "none") {
    loginCard.style.display = "block";
    registerCard.style.display = "none";
  } else {
    loginCard.style.display = "none";
    registerCard.style.display = "block";
  }
}

// Login function
function login(e) {
  e.preventDefault();
  alert("Login successful (demo only)");
}

// Register function
function register(e) {
  e.preventDefault();
  alert("Registration successful (demo only)");
}

async function searchWord() {
  const word = document.getElementById("wordInput").value.trim();
  const defBox = document.getElementById("definition");
  defBox.innerHTML = ""; // Clear previous

  if (word === "") {
    alert("Please enter a word.");
    return;
  }

  try {
    const response = await fetch(`https://api.dictionaryapi.dev/api/v2/entries/en/${word}`);
    if (!response.ok) throw new Error("Word not found");
    const data = await response.json();

    // Display definitions
    let html = `<h3>${data[0].word}</h3>`;
    data[0].meanings.forEach((meaning) => {
      html += `<p><strong>${meaning.partOfSpeech}:</strong> ${meaning.definitions[0].definition}</p>`;
    });
    defBox.innerHTML = html;
  } catch (error) {
    defBox.innerHTML = `<p style="color:red;">Definition not found.</p>`;
  }
}

