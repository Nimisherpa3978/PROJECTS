<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MiniSearch - Google-like Mini Site</title>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Roboto', Arial, sans-serif;
            background: #fff;
            color: #333;
        }

        /* Header */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 30px;
            border-bottom: 1px solid #eaeaea;
        }

        .logo {
            font-size: 24px;
            font-weight: 600;
            color: #4285f4;
            text-decoration: none;
            display: flex;
            align-items: center;
        }

        .logo i {
            margin-right: 8px;
            color: #ea4335;
        }

        .nav-links {
            display: flex;
            gap: 25px;
        }

        .nav-links a {
            text-decoration: none;
            color: #5f6368;
            font-size: 14px;
        }

        .user-icon {
            width: 35px;
            height: 35px;
            border-radius: 50%;
            background: #4285f4;
            color: white;
            display: flex;
            justify-content: center;
            align-items: center;
            font-weight: 500;
        }

        /* Search */
        .search-container {
            max-width: 650px;
            margin: 100px auto 40px;
            padding: 0 20px;
            text-align: center;
        }

        .search-box {
            width: 100%;
            padding: 15px 20px;
            border-radius: 24px;
            border: 1px solid #dfe1e5;
            font-size: 16px;
            margin-bottom: 30px;
            box-shadow: 0 1px 6px rgba(32,33,36,0.1);
        }

        .search-buttons {
            display: flex;
            justify-content: center;
            gap: 15px;
        }

        .search-btn {
            padding: 10px 20px;
            border: 1px solid #f8f9fa;
            background: #f8f9fa;
            border-radius: 4px;
            cursor: pointer;
            font-size: 14px;
        }

        /* Results */
        .results-container {
            max-width: 700px;
            margin: 0 auto 60px;
            padding: 0 20px;
        }

        .results-header {
            font-size: 14px;
            color: #70757a;
            margin-bottom: 25px;
            border-bottom: 1px solid #eaeaea;
            padding-bottom: 10px;
        }

        .result-item {
            margin-bottom: 30px;
        }

        .result-url {
            font-size: 14px;
            color: #202124;
        }

        .result-title {
            font-size: 20px;
            color: #1a0dab;
            text-decoration: none;
            display: block;
            margin: 5px 0;
        }

        .result-description {
            font-size: 14px;
            color: #4d5156;
        }

        /* Footer */
        .footer {
            background: #f2f2f2;
            padding: 20px 30px;
            font-size: 14px;
            color: #70757a;
        }

        .footer-top {
            border-bottom: 1px solid #dadce0;
            padding-bottom: 15px;
        }

        .footer-bottom {
            display: flex;
            justify-content: space-between;
            flex-wrap: wrap;
            padding-top: 15px;
        }

        .footer-links {
            display: flex;
            gap: 20px;
            flex-wrap: wrap;
        }

        .footer-links a {
            color: #70757a;
            text-decoration: none;
        }
    </style>
</head>

<body>

<header class="header">
    <a href="#" class="logo"><i class="fas fa-search"></i> MiniSearch</a>

    <div class="nav-links">
        <a href="#">About</a>
        <a href="#">Images</a>
        <a href="#">News</a>
        <a href="#">Settings</a>
    </div>

    <div class="user-icon">U</div>
</header>

<main>
    <div class="search-container">
        <h1 style="font-size:48px;color:#4285f4;font-weight:400;">MiniSearch</h1>

        <input type="text" class="search-box" id="searchInput" placeholder="Search the web...">

        <div class="search-buttons">
            <button class="search-btn" id="searchBtn">
                <i class="fas fa-search"></i> MiniSearch Search
            </button>
        </div>
    </div>

    <div class="results-container">
        <div class="results-header" id="resultsHeader">
            About 0 results
        </div>

        <div id="resultsList"></div>
    </div>
</main>

<footer class="footer">
    <div class="footer-top">
        <!-- ✅ Country changed -->
        <p>Nepal</p>
    </div>

    <div class="footer-bottom">
        <div class="footer-links">
            <a href="#">Help</a>
            <a href="#">Privacy</a>
            <a href="#">Terms</a>
            <a href="#">Settings</a>
        </div>

        <div class="footer-links">
            <a href="#">Developers</a>
            <a href="#">API</a>
            <a href="#">Contact</a>
        </div>
    </div>
</footer>

<script>
    const data = [
        {
            title: "Introduction to Web Development",
            url: "https://example.com/web",
            desc: "Learn HTML, CSS, and JavaScript for building modern websites."
        },
        {
            title: "JavaScript Basics",
            url: "https://example.com/js",
            desc: "Understand variables, functions, and DOM manipulation."
        },
        {
            title: "CSS Flexbox Guide",
            url: "https://example.com/css",
            desc: "Create responsive layouts easily using Flexbox."
        }
    ];

    const searchBtn = document.getElementById("searchBtn");
    const searchInput = document.getElementById("searchInput");
    const resultsList = document.getElementById("resultsList");
    const resultsHeader = document.getElementById("resultsHeader");

    function search() {
        const q = searchInput.value.toLowerCase();
        resultsList.innerHTML = "";

        const results = data.filter(item =>
            item.title.toLowerCase().includes(q) ||
            item.desc.toLowerCase().includes(q)
        );

        resultsHeader.textContent = `About ${results.length} results`;

        results.forEach(r => {
            resultsList.innerHTML += `
                <div class="result-item">
                    <div class="result-url">${r.url}</div>
                    <a href="#" class="result-title">${r.title}</a>
                    <div class="result-description">${r.desc}</div>
                </div>
            `;
        });
    }

    searchBtn.addEventListener("click", search);
    searchInput.addEventListener("keyup", e => {
        if (e.key === "Enter") search();
    });
</script>

</body>
</html>
